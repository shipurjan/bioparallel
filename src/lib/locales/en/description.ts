import { i18nDescription as Dictionary } from "@/lib/locales/translation";

const d: Dictionary = {
    "Prerendering radius":
        "Radius of the area around the viewport where object are loaded. Decreasing this value will improve performance, but will cause objects (e.g. annotations) to appear with delay around the edge of the screen.",
};

export default d;
